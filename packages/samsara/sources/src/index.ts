// @samsara/sources — Source adapters, one folder per source. Per-source, not per-domain.
// Scaffolded by P0.1. Real exports land in a later task.

export const PACKAGE = "@samsara/sources" as const
